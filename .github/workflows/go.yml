name: Go CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.20'
          cache: true

      - name: Go mod tidy
        working-directory: "./Link Shorter"
        run: go mod tidy

      - name: Build
        working-directory: "./Link Shorter"
        run: go build -v .

      - name: Test
        working-directory: "./Link Shorter"
        run: go test -v ./...
